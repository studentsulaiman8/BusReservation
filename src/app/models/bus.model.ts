export interface BusModel {
  busNumber: number;
  source: string;
  destination: string;
  departureTime: string;
  departureDate: string
  arrivalTime: string;
  arrivalDate: string;
  ticketPrice: number;
  capacity: number;
}
